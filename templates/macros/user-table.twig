{% extends 'layout.twig' %}

{% import 'macros.twig' as macros %}
{#{% import 'macros2.twig' as macros %}#}

{% block title %}
    User table
{% endblock %}
{% block body %}
    <table class="table table-hover">
        <tbody>
        <tr><th>Телефон</th><th>Имя</th><th>Отчество</th><th>Фамилия</th></tr>
        {{ macros.user_table_body(users) }}
        {#
        {% for user in users %}
            {{ macros.user_table_body(user) }}
        {% endfor %}
        #}
        </tbody>
    </table>
{% endblock %}